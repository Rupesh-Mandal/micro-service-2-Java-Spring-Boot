server:
  port: 8005

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Rupesh-Mandal/cinfig-server
          clone-on-start: true
